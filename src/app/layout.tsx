import React from "react"
import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Angel Kisha - Software Engineer",
  description: "Software Engineer Portfolio showcasing projects and experience",
  keywords: 'software engineer, full-stack developer, web development, portfolio, frontend engineer',
  authors: [{ name: 'Angel Kisha' }],
  openGraph: {
    title: 'Angel Kisha - Software Engineer',
    description: 'Software Engineer Portfolio showcasing projects and experience',
    type: 'website',
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" data-theme="dark">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
